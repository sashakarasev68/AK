name: count_divisors-2
limit: 0xFFFF
memory_size: 8192
memory_mapped_io:
  0x80: [2]
  0x84: []
reports:
  - name: "Execution"
    slice: all
    view: |
      {pc}: {instruction:next} {pc:label} | T={T:hex}
  - name: "Result"
    slice: last
    view: |
      numio[0x80]: {io:0x80:dec}
      numio[0x84]: {io:0x84:dec}
    assert: |
      numio[0x80]: [] >>> []
      numio[0x84]: [] >>> [2]
